# Elevar al cuadrado todos los elementos de una lista:
lista = [1, 2, 3, 4, 5]
cuadrados = list(map(lambda x: x ** 2, lista))

print("Lista original:", lista)
print("Lista con elementos elevados al cuadrado:", cuadrados)